import { State } from "../simulator";
import { RenderComponent } from "../types";
import { RaftNode } from "./raftNode";
export declare const raftNodeRing: ({ raftNodes, startAngle, radius, }: {
    raftNodes: RaftNode[];
    startAngle: number;
    radius: number;
}) => RenderComponent<State>;
//# sourceMappingURL=raftNodeRing.d.ts.map